import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddTodo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddTodo container!',
  },
});
