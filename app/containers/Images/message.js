import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Images';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Images container!',
  },
});
